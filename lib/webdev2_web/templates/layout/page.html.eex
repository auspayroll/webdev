<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Webdev2 · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <style>
      .paginator-list li[disabled] a {
        opacity: .4;
        pointer-events: none;
      }

      ul.paginator-list { text-align: center; }
      ul.paginator-list li { display: inline-block; margin: 6px; }
    </style>
  </head>
  <body>
    <header>
      <section class="container">
        <%= link "login", to: SessionRoutes.session_path(@conn, :login) %>
        <%= link "register", to: SessionRoutes.session_path(@conn, :register) %>
      </section>
    </header>
    <main role="main" class="container">
    
      <%= @inner_content %>
    </main>
  </body>
</html>
